import { useCallback, useEffect, useState } from "react";import { apiJson } from "../api/client";import { ErrorBox, FeaturePage, JsonDetails } from "../components/FeatureShell";
export default function SetupPreferencesPage(){const[data,setData]=useState<unknown>();const[error,setError]=useState("");const load=useCallback(async()=>{setError("");try{setData(await apiJson<unknown>("/api/v1/setup/preferences"));}catch(e){setError(e instanceof Error?e.message:String(e));}},[]);useEffect(()=>{const timer=window.setTimeout(()=>void load(),0);return()=>window.clearTimeout(timer);},[load]);return <FeaturePage title="Kurulum Tercihleri" subtitle="Kaydedilmiş PhotoOS setup tercihleri (salt okunur güvenli görünüm)" actions={<button className="feature-button" onClick={()=>void load()}>Yenile</button>}>{error&&<ErrorBox message={error}/>}<article className="feature-card"><JsonDetails value={data}/></article></FeaturePage>}
